- name: install epel
  yum:
    name:
      - epel-release
    state: installed

- name: install mesos repo
  yum:
    name:
      - "http://repos.mesosphere.io/el/7/noarch/RPMS/{{ mesosphere_repo }}"
    state: installed    
