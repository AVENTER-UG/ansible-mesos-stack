---

- name: download and run rexray s3fs plugin
  shell: "docker plugin install --grant-all-permissions {{ rexray_image }} S3FS_ACCESSKEY={{ s3_access_key }} S3FS_SECRETKEY={{ s3_secret_key}} S3FS_ENDPOINT=https://{{ s3_region_endpoint }} S3FS_OPTIONS=url=https://{{ s3_region_endpoint }}"
