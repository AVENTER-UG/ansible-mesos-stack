#Listen on all interfaces
interface=*

addn-hosts=/etc/hosts

# Never forward addresses in the non-routed address spaces.
bogus-priv

server={{ dns1 }}
server={{ dns2 }}

# configure DNS resolution to consul servers
server=/marathon.{{ consul_domain }}/{{ dns1 }}#53
{% for host in groups['manager'] %}
server=/mesos/{{ hostvars[host]['ansible_default_ipv4']['address'] }}#1153
{% endfor %}
server=/service.{{ consul_domain }}/{{ ansible_default_ipv4.address }}#8600

