addn-hosts=/etc/hosts

# Never forward addresses in the non-routed address spaces.
bogus-priv

server={{ dns1 }}
server={{ dns2 }}

# configure DNS resolution to consul servers
server=/weave.local/172.17.0.1#53

